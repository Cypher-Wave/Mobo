{
  "compilerOptions": {
    /* ====== Compilação ====== */
    "target": "ES2020",                   // Padrão moderno do JS gerado
    "module": "CommonJS",                 // Padrão de módulo usado pelo Node.js
    "rootDir": ".",                     // Onde ficam os arquivos .ts
    "outDir": "dist",                     // Onde os .js compilados serão salvos

    /* ====== Tipagem ====== */
    "strict": true,                       // Checagem de tipos rigorosa
    "esModuleInterop": true,              // Permite importar libs CommonJS (como express)
    "resolveJsonModule": true,            // Permite importar arquivos .json
    "skipLibCheck": true,                 // Ignora verificação de tipos em libs externas
    "forceConsistentCasingInFileNames": true,

    /* ====== Node + Paths ====== */
    "moduleResolution": "node",
    "baseUrl": "./src",                   // Caminho base para imports relativos
    "paths": {
      "@config/*": ["config/*"],
      "@controllers/*": ["controllers/*"],
      "@models/*": ["models/*"],
      "@routes/*": ["routes/*"],
      "@services/*": ["services/*"],
      "@utils/*": ["utils/*"],
      "@middlewares/*": ["middlewares/*"]
    },

    /* ====== Output ====== */
    "noEmitOnError": true,                // Impede gerar JS se houver erros
    "sourceMap": true                     // Gera mapas de depuração (debug mais fácil)
  },
  "include": ["src/**/*", "tests/**/*"],                // Inclui tudo dentro de src/
  "exclude": ["node_modules", "dist"]     // Ignora libs e build final
}
